return {
    {
        "ellisonleao/gruvbox.nvim",
        config = function()
            local con = require("gruvbox")
            con.setup({

                contrast = "soft",
            })
        end, 
    },
}
